# Rebuild the project report

From the repo root (uses the trial session in data/sessions and screenshots in docs/screenshots):

```bash
cd docs/report_src
../../.venv/Scripts/python figures.py ../..
../../.venv/Scripts/python build_report.py ../..
```

Output: docs/Career_Mentor_Project_Report.pdf
